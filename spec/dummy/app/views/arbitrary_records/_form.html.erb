<%= form_for(@arbitrary_record) do |f| %>
  <% if @arbitrary_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@arbitrary_record.errors.count, "error") %> prohibited this arbitrary_record from being saved:</h2>

      <ul>
      <% @arbitrary_record.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
